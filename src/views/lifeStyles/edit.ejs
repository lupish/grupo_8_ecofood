<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head', {title: "Editar estilo de vida"}) %>
<body>
    <%- include('../partials/header') %>
    <main>
        <section class="section-common">
            <form class="form-prod-nuevo" method="POST" action="/categoria/edit/<%= categoria.id %>?_method=PUT" enctype="multipart/form-data">
                <h1 class="label-crear-prod">Editar la Categoria</h1>
                <input class="input-form" type="text" name="categoria_nombre" id="categoria-nombre" placeholder="Nombre de la categoría" value="<%=categoria.nombre%>"  required>

                <div class="div-prod-fotos">
                    <p>Foto actual del estilo de vida:</p>
                    <% if(categoria.img) { %>
                        <img class="img-mediana" src="<%= categoria.img.nombre %>" alt="<%= categoria.img.alt %> ">           
                    <% } else { %>
                        <img src="/img/products/img-not-found.webp" alt="Estilo de vida sin imagen">
                    <% } %>
                    <div>
                    <label class="label-prod-fotos" for="estiloVida-foto">Foto de la Categoria: </label>
                    <input class="input-prod-fotos" type="file" name="estiloVida_foto" id="estiloVida-foto">
                </div>
                </div>

                <div>
                    <button type="submit">Editar La categoria</button>
                    <button type="reset">Borrar</button>
                </div>

            </form>
        </section>
    </main>
    <%- include('../partials/footer') %>
</body>
</html>