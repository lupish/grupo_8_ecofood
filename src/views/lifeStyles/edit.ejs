<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head', {title: "Editar estilo de vida"}) %>
<body>
    <%- include('../partials/header') %>
    <main>
        <section class="section-common">
            <form class="form-prod-nuevo" method="POST" action="/lifeStyles/edit/<%= estiloVida.id %>?_method=PUT" enctype="multipart/form-data">
                <h1 class="label-crear-prod">Editar estilo de vida</h1>
                <input class="input-form" type="text" name="estiloVida_nombre" id="estiloVida-nombre" placeholder="Nombre del estilo de vida" value="<%= estiloVida.nombre %>"  required>

                <div class="div-prod-fotos">
                    <p>Foto actual del estilo de vida:</p>
                    <% if(estiloVida.img) { %>
                        <img class="img-mediana" src="<%= estiloVida.img %>" alt="<%= estiloVida.alt %> ">           
                    <% } else { %>
                        <img src="/img/products/img-not-found.webp" alt="Estilo de vida sin imagen">
                    <% } %>
                    <div>
                    <label class="label-prod-fotos" for="estiloVida-foto">Foto del estilo de vida: </label>
                    <input class="input-prod-fotos" type="file" name="estiloVida_foto" id="estiloVida-foto">
                </div>
                </div>

                <div>
                    <button type="submit">Editar estilo de vida</button>
                    <button type="reset">Borrar</button>
                </div>

            </form>
        </section>
    </main>
    <%- include('../partials/footer') %>
</body>
</html>