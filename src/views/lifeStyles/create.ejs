<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head', {title: "Crear estilo de vida"}) %>
<body>
    <%- include('../partials/header') %>
    <main>
        <section class="section-common">
            <form id="form-generico" class="form-prod-nuevo" method="POST" action="/lifeStyles/create" enctype="multipart/form-data">
                <h1 class="label-crear-prod">Crear estilo de vida</h1>
                
                <input class="input-form <%if(locals.errors && errors.estiloVida_nombre){%><%="input-form-no-aceptado"%><%}%>" type="text" name="estiloVida_nombre" id="obj-nombre" placeholder="Nombre del estilo de vida"
                    value="<%= (locals.oldData && oldData.estiloVida_nombre) ? oldData.estiloVida_nombre : "" %>"
                >
                <p id="p-obj-nombre" class="form-error"><%= (locals.errors && errors.estiloVida_nombre) ? errors.estiloVida_nombre.msg : "" %></p>
                
                <div class="div-prod-fotos <%= (locals.errors && errors.estiloVida_foto) ? 'class=input-form-no-aceptado' : "" %>">
                    <label class="label-prod-fotos" for="estiloVida-foto">Foto del estilo de vida: </label>
                    <input class="input-prod-fotos" type="file" name="estiloVida_foto" id="obj-foto">
                    <p id="p-obj-foto" class="form-error"><%= (locals.errors && errors.estiloVida_foto) ? errors.estiloVida_foto.msg : "" %></p>
                </div>

                <div>
                    <button id="form-submit" type="submit">Crear estilo de vida</button>
                    <button type="reset">Borrar</button>
                </div>

            </form>
        </section>
    </main>
    <%- include('../partials/footer') %>
</body>

<script src="/js/frontValidacionesGenericas.js"></script>
</html>